# `@mediadock/contracts`

Source-of-truth DTOs should live in C# (`packages/contracts/csharp`) and be emitted to TypeScript via OpenAPI/NSwag in a later iteration.

MVP: the Angular app uses lightweight hand-written models under `apps/web/src/app/core/models`.
