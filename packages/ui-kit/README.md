# `@mediadock/ui-kit`

PrimeNG wrapper components (design-system surface) to keep feature modules decoupled from raw PrimeNG imports.

MVP: wrappers are not extracted yet; follow the plan and migrate repeated primitives here first.
